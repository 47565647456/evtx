# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies
#
# If you believe there's an error in this file please file an
# issue against the rust-lang/cargo repository. If you're
# editing this file be aware that the upstream Cargo.toml
# will likely look very different (and much more reasonable)

[package]
edition = "2018"
name = "rpmalloc"
version = "0.2.2"
authors = ["Embark <opensource@embark-studios.com>", "Johan Andersson <repi@repi.se>"]
description = "Cross-platform global memory allocator using rpmalloc"
readme = "README.md"
keywords = ["allocator", "rpmalloc"]
categories = ["memory-management", "api-bindings"]
license = "MIT OR Apache-2.0"
repository = "https://github.com/EmbarkStudios/rpmalloc-rs"

[lib]
test = false
doctest = false
[dependencies.rpmalloc-sys]
version = "0.2.2"
features = ["preload"]

[features]
adaptive_thread_cache = ["rpmalloc-sys/adaptive_thread_cache"]
asserts = ["rpmalloc-sys/asserts"]
default = []
global_cache = ["rpmalloc-sys/global_cache"]
guards = ["rpmalloc-sys/guards"]
statistics = ["rpmalloc-sys/statistics"]
thread_cache = ["rpmalloc-sys/thread_cache"]
unlimited_cache = ["rpmalloc-sys/unlimited_cache"]
unlimited_global_cache = ["rpmalloc-sys/unlimited_global_cache"]
unlimited_thread_cache = ["rpmalloc-sys/unlimited_thread_cache"]
validate_args = ["rpmalloc-sys/validate_args"]
